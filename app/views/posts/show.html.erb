<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @post.text %>
</p>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Delete', post_path,
                method: :delete,
                data: { confirm: 'Are you shure delete post?' } %> |
<%= link_to 'Back', post_path %>
